linters:
  enable-all: true
  disable: 
    - golint
    - scopelint
    - interfacer
    - maligned
    - gomoddirectives
    - gci
    - funlen
    - wrapcheck
    - gochecknoglobals
    - goerr113
    - gochecknoinits
    - exhaustivestruct
    - cyclop
    - nestif
    - gocognit
    - structcheck
    - ifshort
    - nosnakecase
    - deadcode
    - varcheck

service:
  golangci-lint-version: 1.57.2

issues:
  new: true
  exclude-dirs:
    - gov/simulation
    - gov/client
    - simulation
  exclude-files:
    - _test.go
    - "test_.*"

linters-settings:
  errcheck:
    check-type-assertions: true
    check-blank: false